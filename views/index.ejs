<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title><%=title%></title>
</head>
<body>
    <ul>
        <% for(var i=0; i<photos.length; i++) {%>
            <li><%= photos[i].name %></li>
        <% } %>
    </ul>
</body>
</html>
