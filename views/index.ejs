<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title><%=title%></title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@glidejs/glide/dist/css/glide.core.min.css">
    <link rel="stylesheet" href="stylesheets/style.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@glidejs/glide/dist/css/glide.theme.min.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
</head>
<body class="bg-dark" style="height: 100vh">

    <div class="row">
        <h5 class="title"><%=title%></h5>
    </div>

    <div class="row justify-content-center h-100 w-100">
        <!-- Formulaire d'ajout de photo -->
        <form class="row mt-5 justify-content-center" action="/upload" method="post" enctype="multipart/form-data">
            <div class="col-auto">
                <label for="image" class="visually-hidden">Email</label>
                <input id="image" class="form-control" type="file" name="image" accept="image/*" onchange="checkFile()">
            </div>
            <div class="col-auto">
                <button id="button" type="submit" class="btn btn-primary mb-3" disabled>Envoyer</button>
            </div>
        </form>

        <!-- Affichage du carrousel -->
        <div class="col-md-10">
            <% if(photos.length) { %>
                <div class="row align-items-center h-50">
                    <div class="col-1">
                        <button class="btn btn-outline-secondary" id="prev">←</button>
                    </div>
                    <div class="col-10">
                        <div class="glide mt-3">
                            <div class="glide__track" data-glide-el="track">
                                <ul class="glide__slides">
                                    <% for(var i=0; i<photos.length; i++) {%>
                                        <li class="glide__slide">
                                            <label class="image-wrap">
                                                <img src="upload/<%=photos[i].image%>" alt="">
                                            </label>
                                        </li>
                                    <% } %>
                                </ul>
                            </div>
                        </div>
                    </div>
                    <div class="col-1">
                        <button class="btn btn-outline-secondary" id="next">→</button>
                    </div>
                </div>
            <!-- Vue si aucune photo n'est upload -->
            <% } else { %>
                <h2 class="title">Aucune photo, veuillez en upload</h2>
            <% } %>
        </div>
    </div>

    <!-- Script librarie glide -->
    <script src="https://cdn.jsdelivr.net/npm/@glidejs/glide"></script>
    <!-- Script de gestion des boutons suivant précèdent pour le carrousel -->
    <% if (photos.length) { %>
        <script type="text/javascript" src="scripts/glider.js"></script>
    <% } %>
    <!-- Script pour gérer le bouton d'upload d'image -->
    <script type="text/javascript" src="scripts/upload.js"></script>
</body>
</html>
